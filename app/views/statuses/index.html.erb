<div class="row">
  <div class="col s12 m12">
    <% @statuses.each do |status| %>
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title"><%= status.user_id %></span>
          <p><%= status.body %></p>
        </div>
        <div class="card-action">
          <a href="#">This is a link</a>
          <a href="#">This is a link</a>
        </div>
      </div>
    <% end %>
  </div>
</div>